image:
  tag: "1.32.0"

domain: "https://borgling-1.tail20def8.ts.net/vaultwarden"

adminToken:
  existingSecret: "vaultwarden-admin-token"
  existingSecretKey: "token"

storage:
  data:
    name: vaultwarden-data
    size: 5Gi
    storageClass: local-path
    accessMode: ReadWriteOnce

ingress:
  enabled: false

resources:
  requests:
    memory: 64Mi
    cpu: 50m
  limits:
    memory: 256Mi
    cpu: 200m

livenessProbe:
  httpGet:
    path: /vaultwarden/alive
    port: 8080

readinessProbe:
  httpGet:
    path: /vaultwarden/alive
    port: 8080
