apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: authelia-postgres-data
  namespace: authelia
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
