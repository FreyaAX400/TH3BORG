apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: speedtest-data
  namespace: speedtest
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
