apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kavita-data
  namespace: kavita
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 2Gi
