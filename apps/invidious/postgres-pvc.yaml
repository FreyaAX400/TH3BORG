apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: invidious-postgres-pvc
  namespace: invidious
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
