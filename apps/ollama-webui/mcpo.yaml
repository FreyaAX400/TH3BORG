apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcpo
  namespace: ollama-webui
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: mcpo
  template:
    metadata:
      labels:
        app: mcpo
    spec:
      nodeSelector:
        kubernetes.io/hostname: borgling-1
      initContainers:
        - name: init-config
          image: busybox:latest
          command: ["sh", "-c", "cp /config-source/config.json /app/config/config.json"]
          volumeMounts:
            - name: config-source
              mountPath: /config-source
            - name: mcpo-config
              mountPath: /app/config
      containers:
        - name: mcpo
          image: ghcr.io/open-webui/mcpo:main
          args: ["--port", "8000", "--config", "/app/config/config.json"]
          ports:
            - containerPort: 8000
          env:
            - name: UV_CACHE_DIR
              value: "/app/uv-cache"
          volumeMounts:
            - name: mcpo-config
              mountPath: /app/config
            - name: uv-cache
              mountPath: /app/uv-cache
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /docs
              port: 8000
            initialDelaySeconds: 300
            periodSeconds: 30
            failureThreshold: 3
      volumes:
        - name: config-source
          configMap:
            name: mcpo-config
        - name: mcpo-config
          persistentVolumeClaim:
            claimName: mcpo-config
        - name: uv-cache
          persistentVolumeClaim:
            claimName: mcpo-uv-cache
---
apiVersion: v1
kind: Service
metadata:
  name: mcpo
  namespace: ollama-webui
spec:
  selector:
    app: mcpo
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP
