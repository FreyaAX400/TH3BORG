apiVersion: apps/v1
kind: Deployment
metadata:
  name: glitchtip-web
  namespace: glitchtip
spec:
  replicas: 1
  selector:
    matchLabels:
      app: glitchtip-web
  template:
    metadata:
      labels:
        app: glitchtip-web
    spec:
      nodeSelector:
        kubernetes.io/hostname: borgling-1
      tolerations:
        - key: node-role.kubernetes.io/control-plane
          operator: Exists
          effect: NoSchedule
      containers:
        - name: glitchtip
          image: glitchtip/glitchtip:latest
          ports:
            - containerPort: 8000
          envFrom:
            - secretRef:
                name: glitchtip-env
          env:
            - name: PORT
              value: "8000"
            - name: EMAIL_URL
              value: consolemail://
            - name: GLITCHTIP_DOMAIN
              value: "https://glitchtip.th3borg.org"
            - name: DEFAULT_FROM_EMAIL
              value: glitchtip@borgling-1.tail20def8.ts.net
          volumeMounts:
            - name: uploads
              mountPath: /code/uploads
      volumes:
        - name: uploads
          persistentVolumeClaim:
            claimName: glitchtip-uploads-pvc
